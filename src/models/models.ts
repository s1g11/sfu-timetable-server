export type ProductGetModel = {
    title: string
}
export type ProductViewModel = {
    id: number
    month: string
    title: string
    description: string
    date: string
    time: string
    place: string
}